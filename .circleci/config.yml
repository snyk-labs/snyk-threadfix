version: 2.1

jobs:
  build:
    docker:
      - image: circleci/python:3.7
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: Build and test
          command: |
            python --version
            pip --version
            ls -la
            pip install poetry
            poetry install -v
            poetry run pytest

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
